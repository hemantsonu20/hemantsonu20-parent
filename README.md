# hemantsonu20-parent
A parent pom for maven based projects under this account

# Steps to release
```
$ mvn clean
$ mvn release:prepare
$ mvn release:perform
$ git push --tags
$ git push origin master
```
